# 研究员沙盒（Research / 情报总监）

- **OpenClaw agent id**：`quant-research`
- **Docker**：`quant-research`，工作目录 `/workspace` 对应该目录。

## TASK_IN.md（由 Seth 写入）

```markdown
## 任务 ID
R-YYYYMMDD-001

## 目标
（例如：调研某因子数据源可行性）

## 交付
- 结论写入 `FINDINGS.md`
- 引用与链接列表

## DoD
- 至少 3 个可验证来源；注明许可与延迟假设
```

执行容器内命令示例见上级 `README.md` 中的 `exec-spoke.ps1`。
